const pkg = require('../package.json');

module.exports = {
  title: pkg.title,
  url: pkg.url,
  project: pkg.name,
  description: pkg.description,
  keywords: pkg.keywords,
  author: pkg.author,
  trackingID: '',
};
